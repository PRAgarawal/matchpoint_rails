<table style="width:80%">
  <tr>
    <th align="left">Date</th>
    <th align="left">Location</th>
    <th align="left">Type</th>
    <th align="left">Players</th>
  </tr>
  <% matches.each do |match| %>
    <tr>
      <td><%= match.match_date.strftime('%a, %b %-d at %-l:%M %p') %></td>
      <td><%= match.court.name %></td>
      <td><%= match.is_singles ? 'Singles' : 'Doubles' %></td>
      <td><%= match.get_player_list %></td>
    </tr>
  <% end %>
</table>
